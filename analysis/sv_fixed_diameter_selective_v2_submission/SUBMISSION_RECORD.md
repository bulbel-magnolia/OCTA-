# Git submission record

This is a new publication record for an already completed analysis. No scientific computation was run and no existing result, figure, README, parameter or validation file was edited.

The user explicitly authorized one independent commit on `analysis/sv-diameter-stage2` and a normal push to `origin/analysis/sv-diameter-stage2`. The initial Git state and every uncommitted file are saved alongside this record. The branch was first fast-forwarded to the existing remote documentation commit; no merge commit, rebase, force-push or history rewrite is used.

`selected_original_files.csv` identifies the completed results and necessary fixed-D inputs. `existing_identity_checks.csv` compares original sealed manifests to current bytes. `.gitattributes` is scoped to these paths to preserve file bytes in Git and keep the original SHA256 manifests valid.

`excluded_uncommitted_files.csv` lists retained local failed intermediate checkpoints and the separate review-delivery artifacts. All successful per-scan integrals, signed full maps, statistical figures and review/validation records are included. Existing manifests still describe the complete local analysis, including excluded diagnostic checkpoints.

The original run status and provenance describe analysis completion before submission and are preserved verbatim. Their earlier no-commit/no-push fields are historical, not a claim about this subsequent submission. The existing undefined-physical-interpretations CSV path reference remains unchanged; the actual JSON result is included.

The final Git commit identifier and remote verification will be reported to the user and recorded locally outside this commit.
